<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- ============================================================ -->
    <!-- CORE-SITE — Hadoop 3.3.4 Lakehouse Configuration            -->
    <!-- ============================================================ -->

    <!-- Default filesystem -->
    <property>
        <name>fs.defaultFS</name>
        <value>hdfs://namenode:9000</value>
    </property>

    <!-- Temporary directory -->
    <property>
        <name>hadoop.tmp.dir</name>
        <value>/opt/hadoop/tmp</value>
    </property>

    <!-- Default permissions -->
    <property>
        <name>fs.permissions.umask-mode</name>
        <value>022</value>
    </property>

    <!-- Proxy user (for Hive) -->
    <property>
        <name>hadoop.proxyuser.hive.hosts</name>
        <value>*</value>
    </property>
    <property>
        <name>hadoop.proxyuser.hive.groups</name>
        <value>*</value>
    </property>

    <!-- Proxy user (for root — dev only) -->
    <property>
        <name>hadoop.proxyuser.root.hosts</name>
        <value>*</value>
    </property>
    <property>
        <name>hadoop.proxyuser.root.groups</name>
        <value>*</value>
    </property>

    <!-- Bind address -->
    <property>
        <name>hadoop.bind.address</name>
        <value>0.0.0.0</value>
    </property>

    <!-- Block size: 128MB -->
    <property>
        <name>fs.local.block.size</name>
        <value>134217728</value>
    </property>

    <!-- Security -->
    <property>
        <name>hadoop.security.auth_to_local</name>
        <value>DEFAULT</value>
    </property>
</configuration>
